import { defineConfig } from 'dumi';

export default defineConfig({
  title: '@clean-js',
  mode: 'site',
  logo: 'logo.png',
  locales: [['zh-CN', '中文']],
  publicPath: '/clean-js/',
  base: '/clean-js/'
});
